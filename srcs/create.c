#include "create.h"
#include <stdio.h>

// Implementação da função
void create_product(const char *name, int idProduct, int amount, float price) {
    printf("Usuário criado: %s, %d anos\n", name, age);
}